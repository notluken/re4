// re4_boot's stub library (docs/port.md, "First boot" milestone, step (d)).
//
// Placeholder: populated from the real undefined-symbol list once re4_boot's first link attempt
// produces one (see docs/port-boot.md and docs/port-phase2.md section 10 for the process). Every
// stub here logs once (so a real call is visible in the trace, not silently swallowed) and returns
// a zero/NULL/no-op value appropriate to its declared signature.
#ifdef TARGET_PC
// Intentionally empty until the first link attempt's undefined-symbol list exists.
#endif
